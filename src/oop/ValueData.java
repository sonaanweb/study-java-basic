package oop;

public class ValueData {
    int value;

    void add(){
        value++;
        System.out.println("숫자 증가 value = " + value);
    }
}

/**
 * 메서드는 객체를 생성해야 호출할 수 있다.
 * static이 붙으면 객체를 생성하지 않고도 메서드를 호출할 수 있다.
 */

/**
 * 인스턴스 메서드 호출 방식은 변수 사용과 동일
 */

/**
 * 데이터가 있는 공간과 메서드가 있는 공간이 같다.
 */